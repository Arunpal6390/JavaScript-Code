// num = 19;
// ans = num > 18 ? "adult" : "minor";//ternary operator
// console.log(ans);
// for of - used in string ,array
// for in - used in object.
name = "biology";
let count = 0;
for (let char of name) {
  count++;
  console.log(`name: ${char}`);
}
console.log(`length of string is: ${count}`);


